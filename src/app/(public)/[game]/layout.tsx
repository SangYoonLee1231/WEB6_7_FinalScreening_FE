import { Fab } from "@/components/common/Fab";

export default function GameLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      {children}
      {/* <Fab /> */}
    </>
  );
}
